Stores ids for [[Element]] and [[Component]]
- Hash map

Process JSON and instantiate all elements using `load` method and components with `build_element`


#### JSON Layout
```json
[
	{
		"name": "main",
		"components": [],
		"elements": []
	}
]
```
